module Finders

  def find_by_name( name )
    # Find a document by name ... 
    puts "asked to find the name: #{name}"
  end

  def find_by_id( doc_id )
    # Find a document by id ...
    puts "asked to find the id: #{doc_id}"
  end

end